export default {
  id: 29,
  title: "Spring Security, JWT & OAuth2",
  range: "445-490",
  concepts: [
    {
      id: 445,
      title: "Importance Of Security",
      intro: "Understand why securing enterprise web applications is not optional.",
      explanation: "Security protects data, restricts unauthorized access, and maintains user privacy. In web systems, exposing endpoints to the world without authentication leaves servers vulnerable to brute-force attacks, data theft, and denial of service.",
      gotchas: [
        "Thinking your backend is safe because it runs on a private network or inside an internal office environment is a major risk."
      ],
      interviewQuestions: [
        {
          question: "Why is security a primary design consideration for enterprise APIs?",
          answer: "To prevent unauthorized access to sensitive user data, protect against exploitation of vulnerabilities, ensure compliance with legal regulations, and prevent malicious actions."
        }
      ],
      code: `// Secure Application Principles:
// 1. Least Privilege (minimal access permissions)
// 2. Defense in Depth (multiple security layers)
// 3. Fail-safe defaults (reject access unless permitted)`,
      visualizerType: "null"
    },
    {
      id: 446,
      title: "OWASP Top 10",
      intro: "Familiarize yourself with the ten most critical web application security risks.",
      explanation: "The Open Web Application Security Project (OWASP) regularly publishes list of top vulnerabilities, including Injection, Broken Authentication, Sensitive Data Exposure, XML External Entities (XXE), and Broken Access Control. Spring Security is designed to mitigate these by default.",
      gotchas: [
        "OWASP is a list of vulnerabilities. Merely using Spring Security does not guarantee your custom business logic is immune to vulnerabilities like Broken Access Control."
      ],
      interviewQuestions: [
        {
          question: "Name 3 vulnerabilities in the OWASP Top 10.",
          answer: "1. Broken Access Control, 2. Cryptographic Failures, 3. Injection (SQL Injection, XSS)."
        }
      ],
      code: `// Top OWASP mitigations handled by Spring Security:
// - SQL Injection: Handled via JPA/PreparedStatement
// - CSRF: Mitigated by default tokens
// - Password storage: BCrypt hashing`,
      visualizerType: "null"
    },
    {
      id: 447,
      title: "Creating A Spring Security Project",
      intro: "Secure your entire Spring Boot application instantly by adding a single starter.",
      explanation: "Adding spring-boot-starter-security dependency automatically locks down all HTTP endpoints in your application. Clients must authenticate using HTTP Basic authentication before accessing any route.",
      gotchas: [
        "As soon as you add this dependency, all your API tests will start failing with HTTP 401 Unauthorized unless credentials are provided."
      ],
      interviewQuestions: [
        {
          question: "What happens when you add spring-boot-starter-security to pom.xml?",
          answer: "Spring Boot automatically registers a DefaultSecurityFilterChain that secures all endpoints, enables form-based login, and generates a random password printed in the console on startup."
        }
      ],
      code: `<!-- Maven dependency -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>`,
      visualizerType: "jvm"
    },
    {
      id: 448,
      title: "Default Login Form",
      intro: "Explore the built-in login interface auto-generated by Spring Security.",
      explanation: "If you attempt to access any endpoint in a browser (like http://localhost:8080/), Spring Security intercepts the request and redirects you to a default login form (`/login`). The default username is 'user' and the password is generated in startup logs.",
      gotchas: [
        "The default login page uses a session-based cookie mechanism. If you clear cookies, you will be redirected to the login form again."
      ],
      interviewQuestions: [
        {
          question: "How do you find the default password in a fresh Spring Security application?",
          answer: "It is dynamically generated during startup and printed in the system console log: 'Using generated security password: XXXX-XXXX-XXXX'."
        }
      ],
      code: `// Console output on startup:
// Using generated security password: 486a427f-94d3-469b-810a-b30f368f5647`,
      visualizerType: "null"
    },
    {
      id: 449,
      title: "Spring Security Filters",
      intro: "Meet the chain of security guards intercepting requests before they reach your controllers.",
      explanation: "Spring Security is implemented as a chain of Servlet Filters (SecurityFilterChain). Every incoming request passes through filters like UsernamePasswordAuthenticationFilter, CsrfFilter, and AuthorizationFilter.",
      gotchas: [
        "If a filter throws an exception, the request is terminated immediately, and the controller is never invoked."
      ],
      interviewQuestions: [
        {
          question: "What is the core architectural mechanism of Spring Security?",
          answer: "A DelegatingFilterProxy that delegates request handling to a chain of Spring-managed Filter beans (SecurityFilterChain)."
        }
      ],
      code: `// Filter Chain Flow:
// HTTP Request -> [ UsernamePasswordAuthenticationFilter ]
//              -> [ CsrfFilter ]
//              -> [ ExceptionTranslationFilter ]
//              -> [ AuthorizationFilter ] -> Controller`,
      visualizerType: "dispatch"
    },
    {
      id: 450,
      title: "Session ID",
      intro: "How the server remembers your authentication status across multiple HTTP requests.",
      explanation: "After successful login via the default form, Spring Security creates an HTTP Session. The server returns a cookie containing a session identifier named JSESSIONID. Subsequent client requests send this cookie, identifying the authenticated session.",
      gotchas: [
        "Stateful sessions (JSESSIONID) are difficult to scale across clustered server environments because sessions are saved in local server memory."
      ],
      interviewQuestions: [
        {
          question: "What is JSESSIONID?",
          answer: "It is a cookie generated by the Servlet container to keep track of a user's session state. Subsequent requests send it back to the server to maintain state."
        }
      ],
      code: `// Response Headers:
// Set-Cookie: JSESSIONID=AB348C9405EF229A88F102B; Path=/; HttpOnly`,
      visualizerType: "null"
    },
    {
      id: 451,
      title: "Setting Username And Password",
      intro: "Override default generated passwords with static credentials inside properties.",
      explanation: "You can configure a simple username and password inside application.properties. This overrides the randomly generated startup credentials, which is useful for local prototyping.",
      gotchas: [
        "Never store plaintext passwords in application.properties for production. Use secure databases and password hashing."
      ],
      interviewQuestions: [
        {
          question: "How do you override default credentials using application.properties?",
          answer: "By setting spring.security.user.name and spring.security.user.password properties."
        }
      ],
      code: `# application.properties
spring.security.user.name=kiran
spring.security.user.password=telusko123`,
      visualizerType: "null"
    },
    {
      id: 452,
      title: "Basic Auth Using Postman",
      intro: "Send HTTP Basic Authorization credentials using API testing tools.",
      explanation: "HTTP Basic authentication involves encoding the 'username:password' string in Base64 and sending it inside the 'Authorization' header (e.g. Authorization: Basic a2lyYW46dGVsdXNrbzEyMw==).",
      gotchas: [
        "Base64 is NOT encryption! Anyone intercepting Basic Auth headers can decode the credentials instantly. Use HTTPS (SSL) to secure transmissions."
      ],
      interviewQuestions: [
        {
          question: "What format does HTTP Basic authentication use to transmit credentials?",
          answer: "It transmits credentials in the 'Authorization' header as a Base64-encoded string prefixed with 'Basic '."
        }
      ],
      code: `// Postman Authorization Header representation:
// Key: Authorization
// Value: Basic a2lyYW46dGVsdXNrbzEyMw==`,
      visualizerType: "null"
    },
    {
      id: 453,
      title: "What Is CSRF?",
      intro: "Cross-Site Request Forgery (CSRF) tricks users into executing malicious actions on logged-in sites.",
      explanation: "CSRF exploits session cookies. If a user is logged into their bank and visits a malicious site, the site can fire a hidden form request to bank.com/transfer. Because the browser automatically attaches the bank's JSESSIONID cookie, the bank processes the request.",
      gotchas: [
        "CSRF protection is only necessary for stateful browser sessions. If your API is completely stateless (e.g. JWT-based), CSRF protection can be disabled."
      ],
      interviewQuestions: [
        {
          question: "Explain how CSRF attacks exploit web browsers.",
          answer: "They rely on the browser's automatic inclusion of session cookies (like JSESSIONID) when sending requests to a domain, allowing malicious sites to submit requests on behalf of the victim."
        }
      ],
      code: `<!-- Malicious form on attacker's site -->
<form action="http://bank.com/transfer" method="post">
    <input type="hidden" name="amount" value="5000">
</form>
<script>document.forms[0].submit();</script>`,
      visualizerType: "null"
    },
    {
      id: 454,
      title: "Error Without CSRF Token",
      intro: "Observe the HTTP 403 Forbidden error triggered when modifying resources without CSRF tokens.",
      explanation: "By default, Spring Security blocks all POST, PUT, and DELETE requests unless a valid CSRF token is provided. If you try to submit a form without it, Spring throws HTTP 403 Forbidden.",
      gotchas: [
        "Clients testing endpoints with Postman will get 403 errors on POST/PUT requests unless CSRF protection is disabled or token is sent."
      ],
      interviewQuestions: [
        {
          question: "Why does a new Spring Security project return 403 Forbidden for POST requests in Postman?",
          answer: "Spring Security enables CSRF protection by default, blocking all state-changing requests (POST, PUT, DELETE) unless a valid CSRF token is included in the headers."
        }
      ],
      code: `// Error response body:
// Status: 403 Forbidden
// Message: "Invalid CSRF Token 'null' was found on the request parameter or header..."`,
      visualizerType: "null"
    },
    {
      id: 455,
      title: "Sending CSRF Token",
      intro: "Acquire and pass the CSRF token from your controller back to client forms.",
      explanation: "For stateful forms, Spring generates a CSRF token. JSPs can fetch it using \${_csrf.token} and inject it inside a hidden form input field. The server verifies this token, validating that the request originated from the client's page.",
      gotchas: [
        "The input field name must be exactly '_csrf' for Spring Security to intercept and validate it successfully."
      ],
      interviewQuestions: [
        {
          question: "How do you include CSRF token inside an HTML form?",
          answer: "By creating a hidden input field named '_csrf' with the token value, e.g. <input type=\"hidden\" name=\"_csrf\" value=\"\${_csrf.token}\">."
        }
      ],
      code: `<form action="/transfer" method="POST">
    <input type="hidden" name="_csrf" value="\${_csrf.token}"/>
    <input type="text" name="amount"/>
    <button type="submit">Submit</button>
</form>`,
      visualizerType: "null"
    },
    {
      id: 456,
      title: "SameSite: Strict",
      intro: "Use SameSite cookie attributes to prevent browsers from attaching cookies to cross-site requests.",
      explanation: "SameSite is a cookie attribute that controls cookie transmission across domains. Setting SameSite=Strict ensures the browser never sends the cookie on cross-site requests, mitigating CSRF attacks.",
      gotchas: [
        "SameSite=Strict can make navigation clumsy, as users clicking links to your site from search engines or emails won't be sent as logged in."
      ],
      interviewQuestions: [
        {
          question: "How does SameSite cookie attribute protect against CSRF?",
          answer: "By telling the browser not to send cookies during cross-site requests (e.g. when origin of request is different than cookie destination domain)."
        }
      ],
      code: `// Cookie header with SameSite attribute:
// Set-Cookie: JSESSIONID=123; Path=/; Secure; HttpOnly; SameSite=Strict`,
      visualizerType: "null"
    },
    {
      id: 457,
      title: "Security Configuration",
      intro: "Define custom security settings by registering a SecurityFilterChain bean.",
      explanation: "In modern Spring, we define custom configurations by declaring a class annotated with @Configuration and @EnableWebSecurity, registering a SecurityFilterChain bean to define which routes are protected and which are public.",
      gotchas: [
        "If you define a custom SecurityFilterChain bean, Spring's auto-configured security rules are fully overridden. You must explicitly configure login and route rules."
      ],
      interviewQuestions: [
        {
          question: "How do you authorize specific URL patterns in Spring Security?",
          answer: "By using authorizeHttpRequests() and configuring matching patterns: requestMatchers(\"/public/**\").permitAll() and anyRequest().authenticated() on the HttpSecurity builder."
        }
      ],
      code: `@Configuration
@EnableWebSecurity
public class SecurityConfig {
    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http.authorizeHttpRequests(auth -> auth
            .requestMatchers("/api/public/**").permitAll()
            .anyRequest().authenticated()
        );
        http.formLogin(Customizer.withDefaults());
        return http.build();
    }
}`,
      visualizerType: "jvm"
    },
    {
      id: 458,
      title: "Disabling CSRF Token",
      intro: "Disable CSRF validation for stateless APIs to simplify developer workflows.",
      explanation: "For stateless REST APIs that use token authorization (like JWT) instead of cookies, CSRF protection is not needed. We disable it using the HttpSecurity configuration.",
      gotchas: [
        "Do not disable CSRF if your client depends on cookie-based session identifiers for authorization."
      ],
      interviewQuestions: [
        {
          question: "When is it safe to disable CSRF protection in Spring Security?",
          answer: "When building stateless APIs that authenticate clients using tokens in headers (like JWT, Bearer tokens) rather than browser-managed session cookies."
        }
      ],
      code: `@Bean
public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
    http.csrf(csrf -> csrf.disable()); // Disables CSRF protection
    return http.build();
}`,
      visualizerType: "jvm"
    },
    {
      id: 459,
      title: "Configuration Without Lambda",
      intro: "Look at the legacy style of configuring Spring Security before Java 8 style lambdas.",
      explanation: "Before lambda syntax was introduced in Spring Security 6.x, configurations chained methods using builder patterns and the `and()` method to separate configuration concerns.",
      gotchas: [
        "Starting with Spring Security 6.0, the legacy chain builder style using `.and()` has been deprecated in favor of clean Lambda-based DSL configurations."
      ],
      interviewQuestions: [
        {
          question: "What configuration style was deprecated in Spring Security 6.0?",
          answer: "The legacy method-chaining style utilizing `.and()` has been deprecated in favor of configuring components using lambda functions."
        }
      ],
      code: `// Legacy Config style (Spring Security 5.x)
http
    .csrf().disable()
    .authorizeRequests()
        .anyRequest().authenticated()
        .and()
    .httpBasic();`,
      visualizerType: "jvm"
    },
    {
      id: 460,
      title: "Getting Ready For User Database",
      intro: "Prepare to fetch users from database tables instead of application properties.",
      explanation: "To secure APIs in production, we need a custom database structure containing user credentials, passwords, and permissions. We will implement interfaces like UserDetailsService to connect JPA with Spring Security.",
      gotchas: [
        "Ensure passwords saved in database are securely hashed before attempting authentication integrations."
      ],
      interviewQuestions: [
        {
          question: "What core interface does Spring Security define to fetch user details?",
          answer: "The UserDetailsService interface, which has a single method: loadUserByUsername(String username)."
        }
      ],
      code: `// User Database Flow:
// Client Credentials -> AuthenticationManager -> AuthenticationProvider
//                     -> UserDetailsService -> Load User from DB -> Compare`,
      visualizerType: "null"
    },
    {
      id: 461,
      title: "Working With Multiple Users",
      intro: "Configure in-memory user details manager to define multiple static test accounts.",
      explanation: "1. InMemoryUserDetailsManager lets you define test user accounts in code — no database needed. Each UserDetails is built with username, password (with encoder prefix like {noop} for plaintext or {bcrypt} for hashed), and roles.\n2. {noop} prefix tells Spring Security to skip encoding — password is compared as-is. Never use {noop} in production. In real apps, store BCrypt hashed passwords.\n3. roles('ADMIN') is shorthand for GrantedAuthority 'ROLE_ADMIN'. Spring Security checks for the 'ROLE_' prefix internally. If you use authorities() instead, you must add 'ROLE_' manually.\n4. You can define multiple beans of type UserDetails and pass them all to InMemoryUserDetailsManager: `new InMemoryUserDetailsManager(user1, user2, user3)`.\n5. In-memory users are destroyed on app restart. They're only for: local sandbox testing, integration test setups (with @TestConfiguration), or tutorial demos. Real apps use database-backed UserDetailsService.",
      gotchas: [
        "In-memory users are deleted as soon as the application restarts. Use them only for development and sandbox testing."
      ],
      interviewQuestions: [
        {
          question: "What is InMemoryUserDetailsManager used for?",
          answer: "It is an implementation of UserDetailsManager that stores user information in-memory, useful for testing and quick local configurations."
        }
      ],
      code: `@Bean
public UserDetailsService userDetailsService() {
    UserDetails user1 = User.withUsername("admin")
                            .password("{noop}admin123") // Plaintext password marker
                            .roles("ADMIN").build();
    UserDetails user2 = User.withUsername("user")
                            .password("{noop}user123")
                            .roles("USER").build();
    return new InMemoryUserDetailsManager(user1, user2);
}`,
      visualizerType: "jvm"
    },
    {
      id: 462,
      title: "Creating User Table and DB Properties",
      intro: "Define the JPA User entity class to manage persistent credentials.",
      explanation: "1. Create a JPA User entity with @Entity @Table(name='users') containing fields: id (auto-generated primary key), username (unique), password (BCrypt hash stored here), and optionally role.\n2. Critical: never name the JPA entity class 'User' without care — it clashes with java.util.Date shadow issues. More critically, never name the table 'user' — it's a reserved SQL keyword in MySQL, PostgreSQL, and Oracle causing syntax errors.\n3. application.properties settings needed: spring.datasource.url, spring.datasource.username, spring.datasource.password, spring.jpa.hibernate.ddl-auto=update (auto-creates the users table).\n4. Add unique constraint on username: @Column(unique = true) — prevents duplicate accounts at the database level. Without this, two users can register with the same username.\n5. Include a 'role' field (VARCHAR) to store ROLE_USER or ROLE_ADMIN. This simplifies authorization — map it to GrantedAuthority in your UserPrincipal class.",
      gotchas: [
        "Avoid using SQL keyword 'user' as your table name directly. Map it to 'users' or 'app_user' using @Table(name = 'users') to prevent database parser exceptions."
      ],
      interviewQuestions: [
        {
          question: "Why should you avoid naming a database table 'user'?",
          answer: "Because 'USER' is a reserved keyword in almost all SQL engines (PostgreSQL, MySQL, Oracle), causing query syntax exceptions."
        }
      ],
      code: `@Entity
@Table(name = "users")
@Data
public class AppUser {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private int id;
    private String username;
    private String password;
}`,
      visualizerType: "jvm"
    },
    {
      id: 463,
      title: "AuthenticationProvider",
      intro: "Customise the authentication provider strategy to leverage database user details.",
      explanation: "1. AuthenticationProvider is Spring Security's interface for verifying credentials. DaoAuthenticationProvider is the default implementation that uses UserDetailsService (to load the user) + PasswordEncoder (to verify the password).\n2. Register it as a @Bean: create DaoAuthenticationProvider, set your custom UserDetailsService with setUserDetailsService(), set PasswordEncoder with setPasswordEncoder(). Spring Security's AuthenticationManager picks it up automatically.\n3. When a login attempt arrives: (1) DaoAuthenticationProvider calls UserDetailsService.loadUserByUsername(username) → (2) compares submitted password with stored hash via PasswordEncoder.matches() → (3) if match, returns authenticated token.\n4. PasswordEncoder is mandatory: without it, DaoAuthenticationProvider throws IllegalStateException. Use BCryptPasswordEncoder in production — never NoOpPasswordEncoder except in quick demos.\n5. Multiple AuthenticationProviders can be registered. Spring Security tries each in order until one succeeds or all fail — useful for supporting both DB users and LDAP directory in the same app.",
      gotchas: [
        "Failing to set the PasswordEncoder on DaoAuthenticationProvider triggers errors when comparing hashed values."
      ],
      interviewQuestions: [
        {
          question: "What is the purpose of DaoAuthenticationProvider?",
          answer: "It is an AuthenticationProvider implementation that retrieves user credentials from a UserDetailsService and validates passwords using a PasswordEncoder."
        }
      ],
      code: `@Bean
public AuthenticationProvider authenticationProvider(UserDetailsService service) {
    DaoAuthenticationProvider provider = new DaoAuthenticationProvider();
    provider.setPasswordEncoder(NoOpPasswordEncoder.getInstance()); // Plaintext comparison (not recommended)
    provider.setUserDetailsService(service);
    return provider;
}`,
      visualizerType: "jvm"
    },
    {
      id: 464,
      title: "Creating a UserDetailsService",
      intro: "Implement UserDetailsService to load credentials from your user repository.",
      explanation: "Create custom service class implementing UserDetailsService. We query our repository to locate the user by username, throwing UsernameNotFoundException if absent.",
      gotchas: [
        "Ensure your custom service class is annotated with @Service so Spring's AuthenticationManager detects and registers it."
      ],
      interviewQuestions: [
        {
          question: "What exception should loadUserByUsername throw if username doesn't exist?",
          answer: "It should throw org.springframework.security.core.userdetails.UsernameNotFoundException."
        }
      ],
      code: `@Service
public class MyUserDetailsService implements UserDetailsService {
    @Autowired
    private UserRepository repo;

    @Override
    public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException {
        AppUser user = repo.findByUsername(username);
        if (user == null) {
            throw new UsernameNotFoundException("User not found: " + username);
        }
        return new UserPrincipal(user); // Wraps entity into UserDetails
    }
}`,
      visualizerType: "jvm"
    },
    {
      id: 465,
      title: "User Repository",
      intro: "Create the query definitions to locate users by their username.",
      explanation: "1. UserRepository extends JpaRepository<AppUser, Integer>, giving you save(), findById(), delete() and all standard CRUD for free — no SQL needed.\n2. Declare `AppUser findByUsername(String username)` — Spring Data JPA generates the SELECT query automatically from the method name. This is called a derived query method.\n3. This method returns null if no user is found (not Optional). If you want safer code, declare: `Optional<AppUser> findByUsername(String username)` and handle the empty case explicitly.\n4. Why not use findById() for login? Because users log in with username (a String), not their integer database ID. findByUsername() is the correct lookup for authentication flows.\n5. Add `@Column(unique = true)` on the username field in AppUser entity to prevent duplicate usernames at the DB level — otherwise two registrations with the same name corrupt your user lookup.",
      gotchas: [
        "Ensure username database column is configured with unique constraint to prevent duplicates."
      ],
      interviewQuestions: [
        {
          question: "Write the query interface to locate a user by name in Spring Data JPA.",
          answer: "AppUser findByUsername(String username);"
        }
      ],
      code: `@Repository
public interface UserRepository extends JpaRepository<AppUser, Integer> {
    AppUser findByUsername(String username);
}`,
      visualizerType: "jvm"
    },
    {
      id: 466,
      title: "UserDetails and UserPrincipal",
      intro: "Bridge your database entity into Spring Security's UserDetails model.",
      explanation: "Spring Security demands UserDetails objects to represent users. We create a wrapper class (e.g. UserPrincipal) implementing UserDetails, passing database AppUser values to override methods like getAuthorities(), getPassword(), and getUsername().",
      gotchas: [
        "Ensure isAccountNonExpired(), isAccountNonLocked(), isCredentialsNonExpired(), and isEnabled() return true, or authentication will fail."
      ],
      interviewQuestions: [
        {
          question: "Why do we create a wrapper class implementing UserDetails?",
          answer: "To decouple database entity structures from Spring Security's authorization requirements without polluting model structures."
        }
      ],
      code: `public class UserPrincipal implements UserDetails {
    private AppUser user;
    
    public UserPrincipal(AppUser user) { this.user = user; }

    @Override
    public Collection<? extends GrantedAuthority> getAuthorities() {
        return Collections.singleton(new SimpleGrantedAuthority("USER"));
    }

    @Override
    public String getPassword() { return user.getPassword(); }

    @Override
    public String getUsername() { return user.getUsername(); }

    @Override
    public boolean isAccountNonExpired() { return true; }
    @Override
    public boolean isAccountNonLocked() { return true; }
    @Override
    public boolean isCredentialsNonExpired() { return true; }
    @Override
    public boolean isEnabled() { return true; }
}`,
      visualizerType: "jvm"
    },
    {
      id: 467,
      title: "Summary Till Now",
      intro: "Verify the database-driven authentication workflow.",
      explanation: "At this stage, you have successfully replaced in-memory settings with a PostgreSQL database, hooked up a custom UserDetailsService, and verified credentials work securely.",
      gotchas: [
        "Ensure you are not still using plaintext passwords in database, as that is highly vulnerable."
      ],
      interviewQuestions: [
        {
          question: "Trace the authentication step when a user attempts login.",
          answer: "User enters username/pass -> Spring's Filter intercepts -> loads database user details via custom UserDetailsService -> validates credentials via AuthenticationProvider -> registers session context."
        }
      ],
      code: `// All database configurations, entities, services, and mappings are fully operational.`,
      visualizerType: "null"
    },
    {
      id: 468,
      title: "What Is BCrypt?",
      intro: "Secure your database by using a mathematically slow, salt-based hashing function.",
      explanation: "BCrypt is a password hashing algorithm that implements a cost factor (work factor), making it computationally expensive. It automatically generates a unique salt for each password to protect against rainbow table attacks.",
      gotchas: [
        "Hashing is a one-way function. You cannot 'decrypt' a BCrypt hash back into the plaintext password."
      ],
      interviewQuestions: [
        {
          question: "How does BCrypt protect passwords against rainbow table attacks?",
          answer: "By incorporating a random salt value. Even if two users choose the exact same password, they produce completely different hashes, rendering database dictionaries useless."
        }
      ],
      code: `// Hashed output structure:
// $2a$10$N9qo8uLOqpGC123...
// $2a$ specifies the version
// $10$ specifies the cost factor (number of hashing rounds)`,
      visualizerType: "null"
    },
    {
      id: 469,
      title: "User Registration",
      intro: "Implement user registration endpoints to capture new accounts.",
      explanation: "1. User registration = accepting a new username+password from the client, encoding the password, and saving the new user to the database via the repository.\n2. Create a POST /register endpoint in a controller. Accept user data as @RequestBody (use a DTO or the entity). Do NOT store the plain-text password.\n3. Registration service flow: (1) Check if username already exists → reject if taken; (2) Encode password: `encoder.encode(rawPassword)`; (3) Set encoded password on user; (4) Call userRepo.save(user).\n4. Return a meaningful response: 201 Created on success with the new user's id (NOT the password). Return 409 Conflict if the username is already taken — don't leak whether an email exists in error messages for security.\n5. Common beginner mistake: forgetting to encode the password before save() — the raw plaintext gets stored, and BCrypt's PasswordEncoder.matches() always returns false on next login because it can't match plaintext to a non-hashed value.",
      gotchas: [
        "Do not store passwords in plain text during registration. Ensure they pass through the encoder first."
      ],
      interviewQuestions: [
        {
          question: "Should you allow duplicate usernames in database registration?",
          answer: "No. Usernames act as unique identifiers. You must verify if a username already exists and throw a descriptive conflict exception."
        }
      ],
      code: `@RestController
public class UserController {
    @Autowired
    private UserService service;

    @PostMapping("/register")
    public AppUser register(@RequestBody AppUser user) {
        return service.saveUser(user);
    }
}`,
      visualizerType: "jvm"
    },
    {
      id: 470,
      title: "BCrypt Encoding for User Registration",
      intro: "Hash passwords with BCrypt before persisting them in your PostgreSQL database.",
      explanation: "1. BCrypt is a one-way hashing algorithm designed for passwords. Unlike MD5/SHA, it is intentionally slow (configurable cost factor) and includes a random salt — making rainbow table and brute-force attacks computationally expensive.\n2. In Spring Security: `BCryptPasswordEncoder encoder = new BCryptPasswordEncoder()`. To hash: `encoder.encode(plainText)`. To verify: `encoder.matches(plainText, hashedFromDB)` — never decrypt, only re-hash and compare.\n3. Each BCrypt hash is different even for the same password because BCrypt embeds a unique random salt in the output string. `encoder.matches()` extracts that salt to reproduce the hash correctly.\n4. Register BCryptPasswordEncoder as a @Bean in your @Configuration class. Inject it wherever you need it (registration service and DaoAuthenticationProvider). Never create it with `new` everywhere — that creates multiple instances unnecessarily.\n5. Cost factor (default 10): controls hash computation time. Increasing to 12 doubles the time per hash — a minor delay for legit users but exponential pain for attackers trying millions of combinations.",
      gotchas: [
        "Make sure to define BCryptPasswordEncoder as a Spring Bean so it is injectable throughout services."
      ],
      interviewQuestions: [
        {
          question: "How do you hash a password using BCrypt in Java?",
          answer: "By instantiating BCryptPasswordEncoder and calling encoder.encode(rawPassword)."
        }
      ],
      code: `@Service
public class UserService {
    @Autowired
    private UserRepository repo;
    
    private BCryptPasswordEncoder encoder = new BCryptPasswordEncoder(12);

    public AppUser saveUser(AppUser user) {
        user.setPassword(encoder.encode(user.getPassword()));
        return repo.save(user);
    }
}`,
      visualizerType: "jvm"
    },
    {
      id: 471,
      title: "Setting Password Encoder",
      intro: "Configure Spring Security to decode and match credentials using BCrypt.",
      explanation: "Declare BCryptPasswordEncoder as a bean in your security config class. The DaoAuthenticationProvider will automatically use this encoder to compare raw user input with database hashes.",
      gotchas: [
        "If you do not register this encoder bean, Spring will default to validating raw strings, failing all BCrypt matches."
      ],
      interviewQuestions: [
        {
          question: "How does Spring Security know to use BCrypt for matching passwords?",
          answer: "By defining a PasswordEncoder bean returning BCryptPasswordEncoder. Spring injects this into DaoAuthenticationProvider."
        }
      ],
      code: `@Configuration
@EnableWebSecurity
public class SecurityConfig {
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder(12);
    }
}`,
      visualizerType: "jvm"
    },
    {
      id: 472,
      title: "Plan To Secure Job App Project",
      intro: "Establish security goals for our Job application before implementation.",
      explanation: "1. The Job App security plan: (a) Add spring-boot-starter-security dependency; (b) Define which endpoints are public vs protected; (c) Configure HTTP session management or switch to stateless JWT.\n2. Endpoints to protect: POST /jobs, PUT /jobs/{id}, DELETE /jobs/{id} — require authentication. GET /jobs — may be public. GET /jobs/{id} — may be public. Admin-only routes need ROLE_ADMIN authorization.\n3. Security configuration class extends SecurityFilterChain bean: define requestMatchers to allow public endpoints, require authentication for others, configure login and logout behavior.\n4. JWT approach for REST APIs: disable session management (stateless), validate JWT on every request via a custom filter, extract username from token to set SecurityContext.\n5. Phased security rollout: (1) Add dependency + test all endpoints now require auth; (2) Configure public/private routes; (3) Add user table + registration; (4) Switch to JWT for stateless API access.",
      gotchas: [
        "Mixing session cookies and API controllers leads to CORS configuration challenges. Let's design a clean security architecture."
      ],
      interviewQuestions: [
        {
          question: "What is the key goal of API authorization planning?",
          answer: "To define distinct roles (like ADMIN or USER) and specify which API endpoints require permissions."
        }
      ],
      code: `// Plan:
// - /register and /login -> permitAll
// - GET /jobs -> authenticated
// - POST/DELETE /jobs -> ADMIN role`,
      visualizerType: "null"
    },
    {
      id: 473,
      title: "Cross-Origin (CORS)",
      intro: "Examine security rules controlling web page interactions across different origins.",
      explanation: "Cross-Origin Resource Sharing (CORS) is a mechanism that uses HTTP headers to tell browsers to run code from one origin on resources from a different origin.",
      gotchas: [
        "Adding @CrossOrigin is insufficient when Spring Security filter chains are active; you must configure CORS rules inside SecurityFilterChain."
      ],
      interviewQuestions: [
        {
          question: "How do you configure CORS inside a Spring SecurityFilterChain?",
          answer: "By calling http.cors() and supplying a CorsConfigurationSource bean containing allowed origins, headers, and methods."
        }
      ],
      code: `http.cors(cors -> cors.configurationSource(request -> {
    CorsConfiguration config = new CorsConfiguration();
    config.setAllowedOrigins(List.of("http://localhost:5173"));
    config.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE"));
    config.setAllowedHeaders(List.of("*"));
    return config;
}));`,
      visualizerType: "jvm"
    },
    {
      id: 474,
      title: "Adding Security Configuration",
      intro: "Integrate routes validation rules within the filter chain.",
      explanation: "1. Spring Security configuration is done via a @Bean of type SecurityFilterChain inside a @Configuration class. This replaces the old WebSecurityConfigurerAdapter (deprecated in Spring Security 5.7+).\n2. Key builder calls: `http.authorizeHttpRequests(auth -> auth.requestMatchers('/public/**').permitAll().anyRequest().authenticated())` — defines which routes are open and which require login.\n3. CSRF: enabled by default, intended for browser form submissions. For stateless REST APIs, disable it: `http.csrf(csrf -> csrf.disable())`.\n4. Form login: `http.formLogin(Customizer.withDefaults())` enables Spring Security's built-in login page. For REST APIs, disable form login and use HTTP Basic or JWT filter instead.\n5. Session management for REST/JWT: `http.sessionManagement(sm -> sm.sessionCreationPolicy(SessionCreationPolicy.STATELESS))` — Spring never creates a session, every request must carry credentials (JWT or Basic Auth header).",
      gotchas: [
        "Ensure matching rules are specified in correct order. Specific rules must go before broad patterns (e.g. `anyRequest()`)."
      ],
      interviewQuestions: [
        {
          question: "Why does the order of request matchers matter in Spring Security?",
          answer: "Spring Security evaluates matchers from top to bottom. If a broad rule like 'anyRequest()' is declared first, subsequent specific rules will be ignored."
        }
      ],
      code: `http.authorizeHttpRequests(auth -> auth
    .requestMatchers("/register", "/login").permitAll()
    .requestMatchers(HttpMethod.DELETE, "/jobs/**").hasRole("ADMIN")
    .anyRequest().authenticated()
);`,
      visualizerType: "jvm"
    },
    {
      id: 475,
      title: "Job App Is Secure Now",
      intro: "Validate configurations to verify routes permissions block unauthorized requests.",
      explanation: "1. At this stage: the Job App has Spring Security configured with user table, BCrypt encoding, and protected endpoints. Only authenticated users can create/update/delete jobs.\n2. Verify security is active: try to call POST /jobs without credentials — you should get 401 Unauthorized. Call it with valid username/password in Basic Auth header — you should get 200 OK.\n3. Test role-based access: create a ROLE_USER and ROLE_ADMIN account. Verify that ROLE_USER cannot access admin-only endpoints (403 Forbidden) while ROLE_ADMIN can.\n4. Common issue: getting 403 instead of 401. 403 means 'you're authenticated but not authorized'. 401 means 'no credentials provided'. Check your security config — permitAll() vs authenticated() vs hasRole() placement.\n5. At this milestone, prepare to replace session-based auth with JWT: sessions don't scale horizontally (each server has different sessions). JWT tokens are stateless — any server can validate them without shared session storage.",
      gotchas: [
        "Ensure the test users are assigned correct roles when database records are seeded."
      ],
      interviewQuestions: [
        {
          question: "How do you check current user roles in Spring security context?",
          answer: "By fetching the Authentication object from SecurityContextHolder.getContext().getAuthentication() and inspecting getAuthorities()."
        }
      ],
      code: `// Secure tests verify:
// GET http://localhost:8080/jobs -> 401 Unauthorized (Without credentials)
// POST http://localhost:8080/register -> 200 OK`,
      visualizerType: "null"
    },
    {
      id: 476,
      title: "Encryption and Decryption",
      intro: "Understand differences between symmetric and asymmetric cryptography.",
      explanation: "Encryption transforms plaintext into ciphertext. Symmetric cryptography uses one shared secret key for encryption and decryption. Asymmetric cryptography utilizes public-private key pairs.",
      gotchas: [
        "Asymmetric encryption is computationally slow compared to symmetric. Use asymmetric to exchange symmetric keys, then use symmetric to encrypt data payloads."
      ],
      interviewQuestions: [
        {
          question: "What is the difference between symmetric and asymmetric encryption?",
          answer: "Symmetric uses the same key to encrypt and decrypt. Asymmetric uses a public key to encrypt and a separate private key to decrypt."
        }
      ],
      code: `// Symmetric: Plaintext + Key ===> Ciphertext ===> Ciphertext + Key ===> Plaintext
// Asymmetric: Plaintext + Public Key ===> Ciphertext ===> Ciphertext + Private Key ===> Plaintext`,
      visualizerType: "null"
    },
    {
      id: 477,
      title: "Digital Signature",
      intro: "Verify authenticity and integrity of messages using digital signatures.",
      explanation: "A digital signature is created by hashing message contents and encrypting the hash using a private key. Recipients decrypt the signature with the matching public key and compare hashes, validating the sender.",
      gotchas: [
        "A digital signature verifies integrity and sender origin; it does not hide the message content itself."
      ],
      interviewQuestions: [
        {
          question: "What does a digital signature guarantee?",
          answer: "1. Authentication (sender identity), 2. Non-repudiation (sender cannot deny transmission), 3. Integrity (message was not modified)."
        }
      ],
      code: `// Signature Flow:
// Sender: Hash(Message) + Private Key = Signature
// Recipient: Decrypt(Signature, Public Key) == Hash(Message)`,
      visualizerType: "null"
    },
    {
      id: 478,
      title: "Why JWT?",
      intro: "Say goodbye to stateful session tables and enable cross-domain scaling.",
      explanation: "JSON Web Tokens (JWT) allow backend APIs to remain completely stateless. Instead of saving active logins in server session memory (which fails to scale across microservices), we sign a token and pass it to the client. The client attaches this token in subsequent requests, and the server validates it cryptographically.",
      gotchas: [
        "JWTs cannot be easily revoked before expiration once issued, unlike stateful sessions which can be killed on the server."
      ],
      interviewQuestions: [
        {
          question: "What is the primary advantage of JWT over session cookies?",
          answer: "Statelessness. The server does not store token state; it validates integrity using signatures, facilitating horizontal scaling."
        }
      ],
      code: `// Stateful: Server tracks Session IDs in memory database
// Stateless (JWT): Server signs token containing roles, Client stores it, Server validates mathematically.`,
      visualizerType: "null"
    },
    {
      id: 479,
      title: "What Is JWT?",
      intro: "Deconstruct the three parts of a JWT: Header, Payload, and Signature.",
      explanation: "A JWT is a dot-separated Base64Url-encoded string comprising three parts: 1. Header (specifies algorithm and token type), 2. Payload (contains claims like username and expiration), 3. Signature (verifies integrity).",
      gotchas: [
        "Anyone can decode Base64Url header and payload blocks. Do not store sensitive info (like passwords or credit card numbers) in token payloads."
      ],
      interviewQuestions: [
        {
          question: "What are the three parts of a JSON Web Token?",
          answer: "Header, Payload, and Signature. They are separated by dots (xxxxx.yyyyy.zzzzz)."
        }
      ],
      code: `// Encoded JWT format:
// header.payload.signature
// base64(Algorithm) . base64(Claims) . HMAC-SHA256(header+payload, SecretKey)`,
      visualizerType: "null"
    },
    {
      id: 480,
      title: "Project Setup for JWT",
      intro: "Add JSON Web Token parsing libraries to your Spring Boot project.",
      explanation: "1. JWT (JSON Web Token) is a stateless authentication mechanism. After login, the server issues a signed token. The client sends this token on every request — the server validates the signature without consulting a database or session store.\n2. Add JWT dependency to pom.xml: io.jsonwebtoken:jjwt-api, jjwt-impl, jjwt-jackson (version 0.11+). These provide token creation, signing, and parsing utilities.\n3. JWT structure: three Base64-encoded parts separated by dots. (1) Header: algorithm type. (2) Payload/Claims: username, expiry, roles. (3) Signature: HMAC of header+payload using a secret key.\n4. application.properties: add a secret key for signing: `app.jwt.secret=yourLongSecretKey`. Set expiry: `app.jwt.expiry=3600000` (1 hour in ms). Load these with @Value in your JWT utility class.\n5. Security filter chain change: disable sessions (STATELESS), disable CSRF, add your JwtAuthFilter before UsernamePasswordAuthenticationFilter — this filter reads the token from the Authorization header on every request.",
      gotchas: [
        "Ensure all jjwt runtime dependencies are included, otherwise ClassNotFoundException occurs when executing parsing code."
      ],
      interviewQuestions: [
        {
          question: "Which popular libraries are used to manage JWT in Java?",
          answer: "jjwt (Java JWT library by Okta/io.jsonwebtoken) and auth0 (by Auth0)."
        }
      ],
      code: `<!-- JJWT Maven Dependencies -->
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.11.5</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.11.5</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.11.5</version>
    <scope>runtime</scope>
</dependency>`,
      visualizerType: "jvm"
    },
    {
      id: 481,
      title: "Custom Login",
      intro: "Create authentication endpoint returning signed JWTs upon successful logins.",
      explanation: "Implement custom POST controller `/login`. We verify credentials using AuthenticationManager. If valid, we invoke our token service to return a signed JWT.",
      gotchas: [
        "Disable form-based logins and session state when enabling custom token configurations."
      ],
      interviewQuestions: [
        {
          question: "How do you programmatically authenticate credentials in Spring Security?",
          answer: "By calling authenticationManager.authenticate(new UsernamePasswordAuthenticationToken(username, password))."
        }
      ],
      code: `@PostMapping("/login")
public ResponseEntity<String> login(@RequestBody AppUser user) {
    Authentication auth = authManager.authenticate(
        new UsernamePasswordAuthenticationToken(user.getUsername(), user.getPassword())
    );
    if (auth.isAuthenticated()) {
        String token = jwtService.generateToken(user.getUsername());
        return ResponseEntity.ok(token);
    }
    return ResponseEntity.status(HttpStatus.UNAUTHORIZED).body("Login Failed");
}`,
      visualizerType: "jvm"
    },
    {
      id: 482,
      title: "Generating Token",
      intro: "Build a JWT helper service to sign user identities cryptographically.",
      explanation: "Create a JWTService class containing helper methods to construct the claims map, set issue and expiration dates, and sign the payload using a secret key.",
      gotchas: [
        "Ensure your secret key is secure and has a length of at least 256 bits (32 bytes) for HMAC-SHA algorithms."
      ],
      interviewQuestions: [
        {
          question: "What details (claims) are commonly included in JWT payloads?",
          answer: "Subject (username), issued-at time (iat), expiration time (exp), and custom attributes like user roles."
        }
      ],
      code: `@Service
public class JWTService {
    private String secretKey = "mySuperSecretKeyForSpringSecurityAuthenticationSystem";

    public String generateToken(String username) {
        Map<String, Object> claims = new HashMap<>();
        return Jwts.builder()
                .setClaims(claims)
                .setSubject(username)
                .setIssuedAt(new Date(System.currentTimeMillis()))
                .setExpiration(new Date(System.currentTimeMillis() + 60 * 60 * 1000)) // 1 hour
                .signWith(SignatureAlgorithm.HS256, secretKey)
                .compact();
    }
}`,
      visualizerType: "jvm"
    },
    {
      id: 483,
      title: "Token Generation Flow",
      intro: "Trace how credentials flow through manager classes to produce tokens.",
      explanation: "AuthenticationManager delegates authentication to AuthenticationProvider. Upon successful validation, the controller executes JWTService.generateToken() to return the JWT string.",
      gotchas: [
        "Any exception during authentication automatically returns HTTP 401 Unauthorized."
      ],
      interviewQuestions: [
        {
          question: "Explain the role of AuthenticationManager in Spring Security.",
          answer: "It acts as the primary coordinator interface, verifying input credentials by routing them to registered AuthenticationProvider instances."
        }
      ],
      code: `// Client POST /login -> AuthenticationManager -> Success -> JWTService.generateToken() -> Client`,
      visualizerType: "null"
    },
    {
      id: 484,
      title: "Creating a JWT Filter",
      intro: "Write a security filter to intercept and authenticate requests containing JWTs.",
      explanation: "Create a filter class extending OncePerRequestFilter. For each request, parse the 'Authorization' header. If it starts with 'Bearer ', extract and validate the token.",
      gotchas: [
        "Ensure your filter handles malformed or expired token exceptions to prevent server crash loops."
      ],
      interviewQuestions: [
        {
          question: "Why should a custom JWT filter extend OncePerRequestFilter?",
          answer: "To guarantee that the filter is executed exactly once per request dispatch cycle."
        }
      ],
      code: `@Component
public class JwtFilter extends OncePerRequestFilter {
    @Autowired
    private JWTService jwtService;
    @Autowired
    private MyUserDetailsService userDetailsService;

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain filterChain)
            throws ServletException, IOException {
        String authHeader = request.getHeader("Authorization");
        String token = null;
        String username = null;

        if (authHeader != null && authHeader.startsWith("Bearer ")) {
            token = authHeader.substring(7);
            username = jwtService.extractUsername(token);
        }
        // Verification step continues next...
        filterChain.doFilter(request, response);
    }
}`,
      visualizerType: "jvm"
    },
    {
      id: 485,
      title: "Setting Auth Token in SecurityContext",
      intro: "Manually authenticate users by setting the security context.",
      explanation: "1. After validating a JWT, you must manually place the authenticated user into Spring Security's SecurityContextHolder — otherwise, Spring doesn't know who the current user is for the rest of the request.\n2. Steps in JwtAuthFilter's doFilterInternal: (1) Extract JWT from 'Authorization: Bearer <token>' header; (2) Validate token and extract username; (3) Load UserDetails via UserDetailsService; (4) Create UsernamePasswordAuthenticationToken with user + authorities; (5) Set it in SecurityContextHolder.\n3. Code: `UsernamePasswordAuthenticationToken authToken = new UsernamePasswordAuthenticationToken(userDetails, null, userDetails.getAuthorities()); SecurityContextHolder.getContext().setAuthentication(authToken);`\n4. Why not set it without loading UserDetails? You need the authorities (roles) from the database. The JWT only carries the username — roles must be re-fetched on each request.\n5. Thread safety: SecurityContextHolder uses a ThreadLocal by default — each HTTP request thread has its own context. The context is cleared automatically after the request completes, preventing auth leakage between requests.",
      gotchas: [
        "If you do not register the authentication object in the context, subsequent security filters will reject the request as unauthorized."
      ],
      interviewQuestions: [
        {
          question: "How do you manually register an authenticated user in Spring Security?",
          answer: "By setting the Authentication object in the SecurityContext: SecurityContextHolder.getContext().setAuthentication(authentication)."
        }
      ],
      code: `if (username != null && SecurityContextHolder.getContext().getAuthentication() == null) {
    UserDetails userDetails = userDetailsService.loadUserByUsername(username);
    if (jwtService.validateToken(token, userDetails)) {
        UsernamePasswordAuthenticationToken authToken = 
            new UsernamePasswordAuthenticationToken(userDetails, null, userDetails.getAuthorities());
        authToken.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
        SecurityContextHolder.getContext().setAuthentication(authToken);
    }
}`,
      visualizerType: "jvm"
    },
    {
      id: 486,
      title: "Validating Token",
      intro: "Implement helper methods to verify token signatures and expiration.",
      explanation: "JWTService parses the token using the secret key. It extracts claims (like username and expiration date) and compares them against current user details.",
      gotchas: [
        "ExpiredJwtException is thrown if the token has expired. Make sure to catch this to send a clean error response."
      ],
      interviewQuestions: [
        {
          question: "What exception is thrown by the parser when parsing an expired JWT?",
          answer: "io.jsonwebtoken.ExpiredJwtException."
        }
      ],
      code: `public boolean validateToken(String token, UserDetails userDetails) {
    final String username = extractUsername(token);
    return (username.equals(userDetails.getUsername()) && !isTokenExpired(token));
}

private boolean isTokenExpired(String token) {
    return extractExpiration(token).before(new Date());
}`,
      visualizerType: "jvm"
    },
    {
      id: 487,
      title: "JWT Summary",
      intro: "Verify the stateless token authentication lifecycle.",
      explanation: "1. JWT flow: (1) POST /login with username+password → server validates → generates and returns JWT; (2) Client stores JWT in localStorage or httpOnly cookie; (3) Client sends JWT in Authorization header for every subsequent request; (4) Server validates JWT signature, extracts username, sets SecurityContext.\n2. JWT vs Session: Sessions store user state server-side (requires sticky sessions or Redis in clusters). JWT is stateless — any server can validate independently, perfect for microservices and horizontal scaling.\n3. JWT security risks: (a) Never store sensitive data in payload — it's Base64 encoded, not encrypted, anyone can decode it; (b) Use HTTPS — JWT in clear HTTP is hijackable; (c) Short expiry + refresh tokens for long-lived sessions.\n4. Token invalidation problem: you can't 'logout' a JWT — it's valid until expiry. Solutions: short expiry (15 min), refresh token rotation, or maintain a server-side blacklist of invalidated tokens (partially defeats statelessness).\n5. Algorithm choice: use HS256 (HMAC SHA-256) for symmetric signing (one secret shared between services) or RS256 (RSA asymmetric) when multiple services need to verify tokens without sharing the signing secret.",
      gotchas: [
        "Ensure your SecurityFilterChain configuration registers the JwtFilter before the UsernamePasswordAuthenticationFilter."
      ],
      interviewQuestions: [
        {
          question: "Why do we add the JwtFilter before the UsernamePasswordAuthenticationFilter?",
          answer: "To ensure that token validation occurs first, setting the security context before the default credential checks are executed."
        }
      ],
      code: `// In SecurityConfig:
http.addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class);`,
      visualizerType: "jvm"
    },
    {
      id: 488,
      title: "Implementing OAuth2",
      intro: "Allow users to log in securely using third-party identity providers.",
      explanation: "OAuth2 is an authorization framework that enables applications to obtain limited access to user accounts on an HTTP service. It delegates authentication to trusted providers (like Google or GitHub).",
      gotchas: [
        "OAuth2 requires registering your application on the provider's developer console to obtain client-id and client-secret credentials."
      ],
      interviewQuestions: [
        {
          question: "What is OAuth2?",
          answer: "OAuth2 is an industry-standard authorization framework that enables third-party applications to access resources on behalf of a user without exposing credentials."
        }
      ],
      code: `<!-- Spring Boot OAuth2 Client Starter -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-oauth2-client</artifactId>
</dependency>`,
      visualizerType: "jvm"
    },
    {
      id: 489,
      title: "Google OAuth2 Login",
      intro: "Integrate Google Sign-In with your Spring application.",
      explanation: "To enable Google Login, configure client-id and client-secret properties. Spring Security automatically creates redirection paths (`/login/oauth2/code/google`).",
      gotchas: [
        "Ensure the redirect URI registered in the Google Developer Console matches your local server redirection URL exactly."
      ],
      interviewQuestions: [
        {
          question: "What properties are needed to configure Google OAuth2 in Spring Boot?",
          answer: "spring.security.oauth2.client.registration.google.client-id and spring.security.oauth2.client.registration.google.client-secret."
        }
      ],
      code: `# application.properties
spring.security.oauth2.client.registration.google.client-id=YOUR_GOOGLE_CLIENT_ID
spring.security.oauth2.client.registration.google.client-secret=YOUR_GOOGLE_CLIENT_SECRET`,
      visualizerType: "null"
    },
    {
      id: 490,
      title: "GitHub Login",
      intro: "Enable GitHub authentication for developers using your application.",
      explanation: "1. GitHub OAuth2 login lets users sign in with their GitHub account — no password stored in your database. Spring Security OAuth2 Client handles the entire OAuth2 Authorization Code flow.\n2. Add dependency: spring-boot-starter-oauth2-client. Register GitHub as a provider in application.properties: spring.security.oauth2.client.registration.github.client-id=xxx and client-secret=yyy.\n3. OAuth2 flow: (1) User clicks 'Login with GitHub'; (2) Redirected to GitHub's auth page; (3) User grants permission; (4) GitHub redirects back with an auth code; (5) Spring exchanges code for access token; (6) Fetches user profile from GitHub API; (7) Creates SecurityContext with GitHub user info.\n4. Security config: add `http.oauth2Login(Customizer.withDefaults())` — Spring Security auto-creates the /login page with the GitHub link and handles the callback at /login/oauth2/code/github.\n5. Custom success handler: implement AuthenticationSuccessHandler to run logic after GitHub login — e.g., auto-register the GitHub user in your own users table on first login, or redirect to a specific page based on their role.",
      gotchas: [
        "Ensure the requested scope matches the profile attributes your application needs to fetch."
      ],
      interviewQuestions: [
        {
          question: "How do you enable OAuth2 login in your SecurityFilterChain configuration?",
          answer: "By calling http.oauth2Login(Customizer.withDefaults()) inside the SecurityFilterChain configuration method."
        }
      ],
      code: `# application.properties
spring.security.oauth2.client.registration.github.client-id=YOUR_GITHUB_CLIENT_ID
spring.security.oauth2.client.registration.github.client-secret=YOUR_GITHUB_CLIENT_SECRET

# Security Config:
# http.oauth2Login(Customizer.withDefaults());`,
      visualizerType: "null"
    }
  ]
};
